# Only needed if your API is not served from the same origin / Vite proxy.
# Leave unset during local dev — vite.config.js already proxies /api to
# http://localhost:4000.
# VITE_API_BASE_URL=http://localhost:4000/api
